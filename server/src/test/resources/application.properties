# Shared integration-test contexts retain their own Hikari pools in Spring's context cache.
# Keep each cached pool small enough to fit PostgreSQL's default connection limit while
# still allowing the existing two-worker concurrency tests to run without serialization.
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=0
