app:
  market-updaters:
    markets:
      bist:
        enabled: ${BIST_ENABLED:true}
        cron: ${BIST_CRON:0/30 15-30 10-18 ? * MON-FRI}
        timezone: ${BIST_TIMEZONE}

spring:
  flyway:
    enabled: true

logging:
  level:
    org:
      hibernate:
        stat: info
        SQL: info

